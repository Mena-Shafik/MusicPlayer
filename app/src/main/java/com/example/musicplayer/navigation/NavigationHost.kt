package com.example.musicplayer.navigation

/**
 * Placeholder for a NavHost setup.
 *
 * To enable navigation, add a NavHost in your Activity's setContent using the
 * `androidx.navigation:navigation-compose` dependency and the `Route` definitions
 * in `Navigation.kt`.
 *
 * Example (in an Activity or composable) — pseudocode:
 *
 * // val navController = rememberNavController()
 * // NavHost(navController, startDestination = Route.Splash.route) { ... }
 */
object NavigationHostPlaceholder
